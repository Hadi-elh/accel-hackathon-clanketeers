[project]
name = "reguline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai==3.19.0",
    "supabase==2.31.0",
]

[tool.vercel]
entrypoint = "app.api:app"
